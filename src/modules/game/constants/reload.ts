/** Reload clip duration after asset trim (`base-animations.glb`). */
export const RELOAD_CLIP_SECONDS = 3;

/** Play reload SFX this many seconds before the clip ends. */
export const RELOAD_SOUND_BEFORE_END_SECONDS = 2;
