export type GameMode = 'team-elimination';

export type RoundPhase = 'live' | 'round-end';

/** Camera rigs cycled with F: first-person, over-the-shoulder, third-person. */
export type CameraMode = 'fps' | 'ots' | 'tps';
