export type GameMode = 'team-elimination';

export type RoundPhase = 'live' | 'loading' | 'countdown' | 'round-end';

/** Camera rigs cycled with C: first-person, over-the-shoulder, third-person. */
export type CameraMode = 'fps' | 'ots' | 'tps';
